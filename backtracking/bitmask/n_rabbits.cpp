#include <bits/stdc++.h>
using namespace std;
vector<vector<int>> grid;
int f(vector<vector<int>>&grid,int i,int n,int mask){
    
}
int main(){
    int n=3;
    grid.resize(n+1,vector<int>(n+1));
    for(int i=1;i<=n;i++){
        for(int j=1;j<=n;j++){
            cin>>grid[i][j];
        }
    }
}